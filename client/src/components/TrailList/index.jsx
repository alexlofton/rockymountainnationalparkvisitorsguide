const TrailList = ({ 
    trail,
    description,
    weather,
    image
}) => {
    // if (blah) {
        // return <h1>No trails, sorry</h1>
    // }
    return (
        <div>
            
        </div>
    )
}

export default TrailList