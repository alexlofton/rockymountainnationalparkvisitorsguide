import { useQuery } from "@apollo/client";

import TrailList from '../components/TrailList'

const Trails = () => {
    return (
        <div>
            <input type="text" />

        </div>
    )
}